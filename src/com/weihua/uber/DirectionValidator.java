package com.weihua.uber;

// Given a few directions, check to see if there is any conflict, for example
// A N C means a is in the northside of c
// C N B means c is north of b
// B N A would be invalid because be cannot be on the north side of b according to the above two rules.
public class DirectionValidator {

    public boolean isValid(char[][] input) {
        return false;
    }
}
